/** Läge 4 — Elevlista (platshållare, byggs i eget arbetsobjekt). */
import { createPlaceholderMode } from "./placeholder.js";

export default createPlaceholderMode({
  id: "elever",
  title: "Elevlista",
  icon: "users",
});
