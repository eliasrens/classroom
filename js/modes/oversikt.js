/** Läge 5 — Översikt (platshållare, byggs i eget arbetsobjekt). */
import { createPlaceholderMode } from "./placeholder.js";

export default createPlaceholderMode({
  id: "oversikt",
  title: "Översikt",
  icon: "chart",
});
